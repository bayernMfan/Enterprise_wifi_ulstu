<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/SecondFragment">

    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.example.enterprise_wifi_ulstu.SecondFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second">
        <action
            android:id="@+id/action_SecondFragment_to_ContactsFragment"
            app:destination="@id/ContactsFragment" />
    </fragment>
    <fragment
        android:id="@+id/ContactsFragment"
        android:name="com.example.enterprise_wifi_ulstu.ContactsFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/contact_details"/>
</navigation>